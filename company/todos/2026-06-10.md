---
date: "2026-06-10"
project: "xguard"
type: daily-todo
---

# TODO - 2026-06-10

## XGuard Top 3

1. [ ] XGuard `7e33e9f Guard production OAuth callback boundary` を `git rebase origin/main` で remote と統合し、tsc --noEmit / targeted Vitest pass 後に push する。（3 日目。今日必ず完了する）
   - 手順: `git fetch origin` → diverge 確認 → `git rebase origin/main` → `git diff --check` → `npx tsc -p tsconfig.json --noEmit` → `npx vitest run --configLoader runner` → `git push origin main`
   - 書き込み不可の場合: `/private/tmp/xguard-midday-2026-06-10` へ `git clone git@github.com:UryuAtsuya/Xguard.git` して rebase → push → 正本を同期
   - force push 禁止。conflict 発生時は Claude Code へ戻す。
   - 証拠: commit hash と push 結果を `company/notes/2026-06-10-midday-xguard-implementation.md` に記録する。
2. [ ] `runtimeConfig.ts` に `PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED` production gate を追加し、`runtimeConfig.test.ts` に 5 ケースのテストを追加して commit / push する。
   - 参照: `company/notes/2026-06-09-morning-planning.md` に手順全文あり（Top 2 セクション）。
   - 完了条件: `git diff --check` + `npx tsc -p tsconfig.json --noEmit` + `npx vitest run --configLoader runner backend/src/__tests__/runtimeConfig.test.ts` 全 pass 後に commit → push。
   - 証拠: targeted Vitest pass + push 完了を `company/notes/2026-06-10-midday-xguard-implementation.md` に記録する。
3. [ ] Supabase CLI 環境確保（CEO 手動タスク・本日期限）
   - CEO が手動で `supabase start` を実行し、`.env.local` に DB URL を設定する。
   - 完了後に Codex へ integration test を委譲する。
   - 未達成の場合は `company/decisions/2026-06-10.md` に延長理由を記録し、期限を 2026-06-12 に更新する。

## 引き継ぎ事項（2026-06-09 夜会から）

- XGuard 正本: `/Users/uryuatsuya/XGuard/xguard`
- GitHub 反映先: `UryuAtsuya/Xguard`
- XGuard local HEAD: `7e33e9f Guard production OAuth callback boundary`（3 日目未 push）
- XGuard remote origin/main（実 GitHub）: `c4403d8 Document XGuard release gates`
- **git 注意**: local と remote が `9ac4f2f` から分岐。`git rebase origin/main` で統合後に push する（force push 禁止）。
- 未追跡: `output/playwright/` 配下の local flow 証跡。Top 3 完了まで触らない。
- `npm run check` は `dist/backend/...` 書き込み `EPERM` で失敗。`tsc --noEmit` / targeted Vitest / `npm run test` は pass。
- 実 Supabase/Postgres integration test は DB URL / `psql` 条件不足で skip 継続。
- Codex 昼 run は 3 日連続未実施。今日は必ず実施する。

## CEO 決定事項（参照: company/decisions/2026-06-09.md）

- Codex 昼 run 未実施が継続する場合は CEO が手動で rebase + push を実行する。
- Supabase CLI 環境確保は今日（2026-06-10）が期限。未達成は 2026-06-12 延長 + 記録必須。
- OAuth live credentials 検証は runtime gate 完了後のフェーズへ（継続）。
- `output/playwright/` は Top 3 全完了後に `.gitignore` 追加を判断する（継続）。
- force push 禁止は継続。

## 追加候補（優先順 低め）

- [ ] proof visibility endpoint に監査/compliance event 記録を追加する。
- [ ] OAuth state repository を Supabase または Redis へ移し、TTL 付き atomic consume にする。
- [ ] configured OAuth frontend を `authorizationUrl` へ遷移させ、frontend test を追加する。
- [ ] `output/playwright/` 未追跡ファイルの commit 要否を判断する。
