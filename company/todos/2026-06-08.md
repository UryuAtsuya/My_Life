# TODO - 2026-06-08

## XGuard Top 3

1. [ ] OAuth configured modeで実X token exchangeを実装し、取得tokenのsubject/account検証後だけsessionを発行する。mock callback/session発行はproductionで起動禁止にする。
   - 部分対応: `7e33e9f Guard production OAuth callback boundary`（ローカルのみ、未 push）。境界 gate は追加済みだが live credentials 検証は未完。P0 継続。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、role、ownership、同一Xアカウント、存在しない `backup_run`、`x_account_id` 必須、負値、月次上限超過を証跡化する。
   - blocker: DB URL / `psql` 条件不足で skip 継続（2026-05-28 以降）。
3. [ ] `docs/API_COST_MODEL.md` と `docs/COMPLIANCE.md` を更新し、pricing confirmed、spending limit、24時間削除SLA、API access終了時全削除runbookをCI/runtime release gateへ落とす。
   - docs 更新: `c4403d8` で完了済み。runtime gate 実装（`PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED`）は未着手。Codex 昼実装未実施のため未完。

## 19:00 Closeout 確認（2026-06-08）

- Codex 昼実装 run なし（実施証跡なし）。夕方レビュー run なし。
- 朝 handoff note `company/notes/2026-06-08-claude-code-codex-handoff.md` 作成済み（未 commit）。
- XGuard HEAD: `7e33e9f`（ローカルのみ）。remote diverge 解消は明日。

## 引き継ぎ事項（2026-06-07 夜会から）

- XGuard正本: `/Users/uryuatsuya/XGuard/xguard`
- GitHub反映先: `UryuAtsuya/Xguard`
- XGuard local HEAD: `7e33e9f Guard production OAuth callback boundary`（未 push、origin/main [ahead 1]）
- XGuard remote origin/main: `c4403d8 Document XGuard release gates`（昨日昼 temp clone から push 済み）
- **git 注意**: local と remote が `9ac4f2f` から分岐。Codex は fetch → rebase 後に push すること。
- proof visibility management endpointは `9ac4f2f` に含まれる。targeted Vitest 11件はpass。
- 未追跡: `output/playwright/` 配下のlocal flow証跡。Top 3完了までは触らない。
- `npm run check` は `dist/backend/...` 書き込み `EPERM` で失敗。`tsc --noEmit`、targeted Vitest、Verification agentの `npm run test` はpass。
- 実Supabase/Postgres integration testはDB URL / `psql` 条件不足でskip。

## 12:45 Codex ハンドオフ

- **昼 Codex 対象**: Top 3 — `runtimeConfig.ts` に `PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED` production gate を追加する。
- **理由**: Top 1 は live X credentials 要、Top 2 は DB URL 要。Top 3 は外部依存なし・完了検証容易。
- **指示全文**: `company/notes/2026-06-08-claude-code-codex-handoff.md`

## 追加候補（優先順 低め）

- [ ] proof visibility endpointに監査/compliance event記録を追加する。
- [ ] OAuth state repositoryをSupabaseまたはRedisへ移し、TTL付きatomic consumeにする。
- [ ] configured OAuth frontendを `authorizationUrl` へ遷移させ、frontend testを追加する。
- [ ] `output/playwright/` 未追跡ファイルのcommit要否を判断する。
