---
date: "2026-06-15"
project: "xguard"
type: daily-todo
---

# TODO - 2026-06-15

## XGuard Top 3

1. [x] proof visibility revoke 時の compliance event 記録を追加する。
   - branch: `feature/proof-revocation-audit`
   - base: `030a9164df301cf01a47bd5ecfbfe0033e973e9c`
   - commit: `303bd34 Record proof revocation compliance events`
   - 初回の成功した owner revoke のみ `proof_page_revoked` event を1件記録する。
   - rejected request、idempotent retry、revoked 後の再公開では event を追加しない。
   - `git diff --check`、`tsc --noEmit`、targeted Vitest 13件、`npm run check` が pass。
2. [x] `content_compliance_events` repository境界を実装する。
   - branch: `feature/content-compliance-events-repository`
   - commit: `ce4de54 Add compliance event repository`
   - proof revoke の `proof_page_revoked` 記録を process-local 配列から `ContentComplianceEventRepository` 経由へ移した。
   - in-memory 実装と Supabase row mapping 実装を追加した。
   - `git diff --check`、`tsc --noEmit`、targeted Vitest 15件、`npm run check` が pass。
3. [ ] Supabase CLI/Postgres integration 環境を確保する。
   - `supabase` / `psql` が未検出。
   - `DATABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` が未設定。
   - 実 Supabase/Postgres integration test は blocked。
   - proof revoke state と compliance event の真の同一transaction確認は、proof page永続repository統合時に行う。

## Release Status

- `develop = origin/develop = cdcd5b4`
- `feature/proof-revocation-audit = origin/feature/proof-revocation-audit = 303bd34`
- `feature/content-compliance-events-repository = origin/feature/content-compliance-events-repository = ce4de54`
- production release: No-Go 継続
- `output/playwright/` の未追跡成果物は変更なし
