# TODO - 2026-06-07

## XGuard Top 3

1. [x] XGuard正本パスの unstaged changes（`backupProofAuth.test.ts`・`app.ts`・`API_SPEC.md`）を `git add` / `git commit` / `git push origin main` する。Evidence: `4bd28a7 Keep proof pages private by default` を `UryuAtsuya/Xguard` `main` へpush済み。`tsc --noEmit` と targeted Vitest pass。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、role・ownership・同一Xアカウント・存在しない `backup_run`・`x_account_id` 必須・負値・月次上限超過を確認する。Codex委譲。
3. [ ] `docs/API_COST_MODEL.md` に通常read単価・`Owned Reads` 非適用・Usage endpoint・spending limit・月次上限前停止を反映し、`docs/COMPLIANCE.md` にEnterprise適用要否・24時間削除SLA・API access終了時全削除runbookを反映する。Codex委譲。

## 夜会結果

- XGuard `4bd28a7 Keep proof pages private by default` をpush済み。
- 実Supabase/Postgres integration testはDB URL / `psql` 条件不足でskip。
- `docs/API_COST_MODEL.md` / `docs/COMPLIANCE.md` release gate更新は未完のまま明日へ送る。
- Review agentがP0として、configured OAuth callbackの実token exchange未実装を指摘。明日のTop 1へ繰り上げる。

## 引き継ぎ事項（2026-06-06 夜会から）

- XGuard正本 `/Users/uryuatsuya/XGuard/xguard`
- `HEAD = origin/main = b03d9c8 Protect backup and proof APIs`（push済み）
- 未コミット unstaged changes あり: `backend/src/__tests__/backupProofAuth.test.ts`（テスト拡充）、`backend/src/app.ts`（visibility private defaultへ修正）、`docs/API_SPEC.md`（auth境界をドキュメント化）
- `output/playwright/` 未追跡ファイルあり（今日も触らない）
- `npm run build:api` / `npm run build:web` は canonical path で `EPERM`。Codexはwritable clone使用可。
- 全Vitestは frontend `App.test.tsx` 5秒timeout 1件で失敗するが、`--testTimeout=20000` targeted ではpass（環境遅延）。
- `git ls-remote origin` は前回DNS失敗だったが `b03d9c8` はpush済み確認済み。

## 追加候補（優先順 低め）

- [ ] P2: owner management responseとHTTP境界テストをさらに整理する。
- [ ] `output/playwright/` 未追跡ファイルのcommit要否を確認する（commit対象か削除対象か）。
- [ ] Supabase Auth/JWT + DB ownershipで prototype user headerを置換する（release gate）。
- [ ] configured OAuth frontendを `authorizationUrl` へ遷移させ、frontend testを追加する。
- [ ] token repositoryとSupabase schemaの保存契約を一本化する。
- [ ] `Free診断 / 1,980円 Personal / 2,980円 Pro / 4,980円 Concierge検証枠` のLP文言案を作る。
- [ ] 初期ヒアリング対象を、X中心creator・仕事用X小規模事業者・創作/成人向け/グレー領域creatorから各3人ずつ仮リスト化する。
