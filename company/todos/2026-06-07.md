# TODO - 2026-06-07

## XGuard Top 3

1. [ ] backup / proof APIへ認証、user ownership、proof visibility/revocation境界を追加し、他user・private・revoked proof拒否テストを入れる。Codexへ委譲。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、role・ownership・同一Xアカウント・存在しない `backup_run`・`x_account_id` 必須・負値・月次上限超過を確認する。
3. [ ] `docs/API_COST_MODEL.md` に通常read単価・`Owned Reads` 非適用・Usage endpoint・spending limit・月次上限前停止を反映し、`docs/COMPLIANCE.md` にEnterprise適用要否・24時間削除SLA・API access終了時全削除runbookを反映する。

## 引き継ぎ事項

- XGuard canonical path `origin/main` = `2b96993 Add OAuth state and PKCE guard`。tracked差分なし、`output/playwright/` 未追跡あり。
- `git ls-remote` はDNS失敗、`git fetch` は `.git/FETCH_HEAD` 書き込み不可。live remote確認は未完。
- `npm run build:api` / `npm run build:web` は書き込み権限 `EPERM`。CIまたはwritable checkoutで `npm run check` を再実行する。
- 全Vitestはfrontend 5秒timeout 1件で失敗したが、frontend targeted は `--testTimeout=20000` でpass。
