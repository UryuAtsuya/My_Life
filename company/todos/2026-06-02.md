# TODO - 2026-06-02

## XGuard Top 3

1. [x] `/Users/uryuatsuya/XGuard/xguard` のdirty差分を読み、昨日push済み記録 `8aa0910` とlive remote正本の関係を確認する。昼runでは `HEAD=origin/main=4e6258c`、dirty差分は `backup_run_id` 付きusage eventの `x_account_id` 必須化と判定。夜run最終状態では `HEAD=origin/main=95e6392` で、`86a71fb` と `8aa0910` をmerge済み。live GitHubの独立確認はDNS失敗で未完了。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`service_role` 実行、`authenticated` 拒否、ownership、同一Xアカウント整合性、存在しない `backup_run`、`x_account_id` 必須、月次上限超過、負値拒否を確認する。
3. [ ] OAuth configured modeの静的 `state`、plain/mock PKCE、callback未照合を解消し、token repositoryとSupabase schemaの保存契約を一本化する。Developer Console原価確認も `docs/API_COST_MODEL.md` へ反映する。

## 追加候補

- [x] `33cae26 Require X account for backup usage events` をremote先行分へrebase/cherry-pickし、再検証後に `UryuAtsuya/Xguard` `origin/main` へpushする。夜run最終状態では指定パスが `95e6392 Merge remote-tracking branch 'origin/main'` まで進み、local `origin/main` と一致。
- [ ] `/api/x/oauth/status` をproductionで無効化、またはadmin-only/deployment-only health checkへ寄せる。
- [ ] CORSを `APP_BASE_URL` または明示allowlistベースへ寄せる。
- [ ] `/Users/uryuatsuya/XGuard/xguard` の `writable=no` と `.git/FETCH_HEAD` 書き込み不可を解消し、canonical checkoutをlive remote正本へ同期する。
