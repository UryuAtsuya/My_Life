# TODO - 2026-06-02

## XGuard Top 3

1. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`service_role` 実行、`authenticated` 拒否、ownership、同一Xアカウント整合性、存在しない `backup_run`、月次上限超過、負値拒否を確認する。
2. [ ] OAuth configured modeの静的 `state`、plain/mock PKCE、callback未照合を解消し、token repositoryとSupabase schemaの保存契約を一本化する。
3. [ ] Developer Consoleでendpoint別単価、credit/spending、Usage endpoint、Owned Reads条件を確認し、XGuard `docs/API_COST_MODEL.md` とcompany gateへ反映する。

## 追加候補

- [ ] `/api/x/oauth/status` をproductionで無効化、またはadmin-only/deployment-only health checkへ寄せる。
- [ ] CORSを `APP_BASE_URL` または明示allowlistベースへ寄せる。
- [ ] `/Users/uryuatsuya/XGuard/xguard` の `writable=no` と `.git/FETCH_HEAD` 書き込み不可を解消し、canonical checkoutを `8aa0910` へ同期する。
