# TODO - 2026-05-31

## XGuard Top 3

1. [ ] 指定パス `/Users/uryuatsuya/XGuard/xguard` の未コミット変更4ファイルを読んで、今日のOAuth/API検証範囲に取り込むか、別作業として避けるかを決める。
2. [ ] 実Supabase/Postgres migration testで `record_api_usage_event_with_monthly_limit` の `service_role` 実行可、`authenticated` 実行不可、所有関係、同一Xアカウント整合性、月次上限超過、負値拒否を確認する。
3. [ ] real OAuth configured modeとDeveloper Console原価実値をsecret非表示で確認し、callback URL、scope、endpoint別単価、credit/spending設定、Usage endpoint、Owned Reads条件を `docs/API_COST_MODEL.md` とcompany記録へ反映する。

## 引き継ぎ

- XGuard正本: `/Users/uryuatsuya/XGuard/xguard`
- XGuard remote: `UryuAtsuya/Xguard` `origin/main`
- 最新push済みcommit: `d30fc48 Harden Supabase usage ledger boundary`
- 2026-05-31朝確認: 指定パスは `exists=yes`, `writable=no`, `main...origin/main`, HEAD `d30fc48`。未コミット変更は `backend/src/__tests__/api.test.ts`, `backend/src/app.ts`, `docs/API_SPEC.md`, `docs/DEPLOY.md`。
- 昼run最初にwrite/fetchを再確認し、書けなければ `/private/tmp/xguard-midday-2026-05-31` をremote最新から作る。Vaultへ実装コードを置かない。
- v0 scopeは `tweet.read`, `users.read`, `offline.access` のみ。`follows.read`, DM/write/follow系scopeは入れない。

## 後回し

- note記事制作・公開判断
- Today Board改善
- Short Video Operations OS
