# TODO - 2026-06-01

## XGuard Top 3

1. [ ] GitHub DNS/権限復旧後、`/Users/uryuatsuya/XGuard/xguard` で `git ls-remote origin refs/heads/main` と `git fetch origin main` を再実行し、`552f2e5 Add OAuth status diagnostic endpoint` が `UryuAtsuya/Xguard` `origin/main` に実在するか確認する。
2. [ ] 一時cloneの `09ff660 Add OAuth status endpoint` と指定パスの `552f2e5 Add OAuth status diagnostic endpoint` を比較し、`552f2e5` が上位差分なら `09ff660` を破棄、必要差分があればcherry-pickで統合する。
3. [ ] 実Supabase/Postgres migration testとDeveloper Console原価実値確認を閉じる。確認対象は `service_role` 専用実行、`authenticated` 拒否、所有関係、同一Xアカウント整合性、月次上限超過、負値拒否、endpoint別単価、spending limit、Usage endpoint、Owned Reads条件。

## 追加候補

- [ ] `/api/x/oauth/status` は本番公開前にadmin認証付きhealth checkまたはdeployment-only routeへ寄せる。
- [ ] OAuth start/callbackの静的 `state`、mock PKCE、mock token refs保存を、real token exchange実装へ進める。
- [ ] configured modeでも `X_CLIENT_SECRET` 欠落を `missingEnv` に出す。ただしsecret値は返さない。
- [ ] route testをExpress内部構造依存からHTTP境界テストへ戻す。
