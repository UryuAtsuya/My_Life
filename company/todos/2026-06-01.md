# TODO - 2026-06-01

## XGuard Top 3

1. [ ] GitHub DNS/権限復旧後、`/private/tmp/xguard-midday-2026-06-01-1331` の `8cf029c Harden usage ledger schema contract` をfetch/rebase確認し、`UryuAtsuya/Xguard` `origin/main` へpushする。force pushしない。
2. [ ] 実Supabase/Postgres migration testを閉じる。確認対象は `record_api_usage_event_with_monthly_limit` の `service_role` 専用実行、`authenticated` 拒否、所有関係、同一Xアカウント整合性、`backup_run_id` 付きで `x_account_id` なし、存在しない `backup_run`、月次上限超過、負値拒否。
3. [ ] Developer Console原価実値確認を閉じる。endpoint別単価、credit残高、spending limit、Usage endpoint、Owned Reads条件を確認し、`docs/API_COST_MODEL.md` とcompany gateへ反映する。

## 追加候補

- [x] `record_api_usage_event_with_monthly_limit` のlocal schema contract testを追加し、`service_role` grant / `authenticated` revoke / ownership / monthly limit / 負値拒否のschema前提をCIで検知できるようにする。XGuard local commit: `8cf029c`。
- [ ] 一時cloneの `09ff660 Add OAuth status endpoint` は、`2655267` 系列に必要差分がなければ破棄する。必要差分があればcherry-pickで統合する。
- [ ] `/api/x/oauth/status` は本番公開前にadmin認証付きhealth checkまたはdeployment-only routeへ寄せる。
- [ ] OAuth start/callbackの静的 `state`、mock PKCE、mock token refs保存を、real token exchange実装へ進める。
- [ ] configured modeでも `X_CLIENT_SECRET` 欠落を `missingEnv` に出す。ただしsecret値は返さない。
- [ ] route testをExpress内部構造依存からHTTP境界テストへ戻す。
