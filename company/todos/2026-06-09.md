# TODO - 2026-06-09

## XGuard Top 3

1. [ ] XGuard `7e33e9f Guard production OAuth callback boundary` を `git rebase origin/main` で remote と統合し、tsc --noEmit / targeted Vitest pass 後に push する。
   - 手順: `git fetch origin` → diverge 確認 → `git rebase origin/main` → 検証 → `git push origin main`
   - 証拠: commit hash と push 結果を `company/notes/2026-06-09-midday-xguard-implementation.md` に記録する。
2. [ ] `runtimeConfig.ts` に `PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED` production gate を追加し、`runtimeConfig.test.ts` に 5 ケースのテストを追加して commit / push する。
   - 参照: `company/notes/2026-06-08-claude-code-codex-handoff.md` に指示全文あり。
   - 証拠: targeted Vitest pass + push 完了を記録する。
3. [ ] 実 Supabase/Postgres integration test のブロッカー解消案を確定する。
   - CEO タスク: ローカルで `supabase start` を試し、`.env.local` に DB URL を設定できるか確認する。
   - 確認後に Codex へ委譲する。

## 引き継ぎ事項（2026-06-08 夜会から）

- XGuard 正本: `/Users/uryuatsuya/XGuard/xguard`
- GitHub 反映先: `UryuAtsuya/Xguard`
- XGuard local HEAD: `7e33e9f Guard production OAuth callback boundary`（未 push）
- XGuard remote origin/main: `c4403d8 Document XGuard release gates`
- **git 注意**: local と remote が `9ac4f2f` から分岐。`git rebase origin/main` で統合後に push する（force push 禁止）。
- 未追跡: `output/playwright/` 配下の local flow 証跡。Top 3 完了まで触らない。
- `npm run check` は `dist/backend/...` 書き込み `EPERM` で失敗。`tsc --noEmit` / targeted Vitest / `npm run test` は pass。
- 実 Supabase/Postgres integration test は DB URL / `psql` 条件不足で skip 継続。

## CEO 決定事項（参照: company/decisions/2026-06-08.md）

- `7e33e9f` は rebase + verify + push の順で処理。conflict 発生時は Claude Code へ戻す。
- Supabase CLI 環境確保の期限は 2026-06-10 目標（CEO が手動で `supabase start` を試す）。
- OAuth live credentials 検証は runtime gate 完了後のフェーズへ。production No-Go 継続。
- `output/playwright/` は Top 3 完了後に `.gitignore` 追加を判断する。

## 追加候補（優先順 低め）

- [ ] proof visibility endpoint に監査/compliance event 記録を追加する。
- [ ] OAuth state repository を Supabase または Redis へ移し、TTL 付き atomic consume にする。
- [ ] configured OAuth frontend を `authorizationUrl` へ遷移させ、frontend test を追加する。
- [ ] `output/playwright/` 未追跡ファイルの commit 要否を判断する。
