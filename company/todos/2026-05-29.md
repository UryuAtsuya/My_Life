# TODO - 2026-05-29

## Top 3

- [x] XGuard指定パス `/Users/uryuatsuya/XGuard/xguard` のwrite/fetch/remote状態を確認する。結果: `XGUARD_NOT_WRITABLE`、HEAD/cached `origin/main` は `455718c`、fetchは `.git/FETCH_HEAD` 書き込み不可。
- [x] 昨日の一時clone `/private/tmp/xguard-midday-2026-05-28` の `9be85a1` と指定パスの `c0a7dcd` を比較し、未反映のproduction boundaryだけを `/private/tmp/xguard-midday-2026-05-29-localref` へ取り込む。XGuard local commit: `3120411`。
- [ ] real OAuth configured modeとDeveloper Console原価確認を実施し、`docs/API_COST_MODEL.md`、PM ticket、project noteへ同期する。昼runではGitHub remote先行 + DNS失敗のため未完了。

## XGuard昼実装引き継ぎ

- 実装コードはMyLife Vaultへ置かない。
- 指定パスがwritableなら `/Users/uryuatsuya/XGuard/xguard` で作業する。
- 指定パスがwritableでない場合は `/private/tmp/xguard-midday-2026-05-29` をremote最新から作る。
- v0 scopeは `tweet.read`, `users.read`, `offline.access` のみ。
- `follows.read`, `dm.read`, `dm.write`, `tweet.write`, `follows.write` はv0に入れない。
- 検証は `git diff --check`, `npx tsc -p tsconfig.json --noEmit`, `npx vitest run --configLoader runner`, `npm run check`, `git diff --cached --check` を基本にする。

## XGuard昼実装結果

- 作業コピー: `/private/tmp/xguard-midday-2026-05-29-localref`
- XGuard local commit: `3120411 Harden Supabase usage ledger boundary`
- push: 未完了。`git push origin main` は `fetch first`、`git fetch origin main` / `git ls-remote origin refs/heads/main` は `Could not resolve host: github.com`。
- 検証: `npm ci`, `tsc --noEmit`, targeted Vitest（4 files / 34 tests）, `npm run check`（6 files / 37 tests）, `git diff --check`, `git diff --cached --check` pass。

## XGuard夜レビュー結果

- [x] 夜runで `/Users/uryuatsuya/XGuard/xguard` のwrite/fetch/push状態を再確認する。結果: `XGUARD_WRITABLE`、`.git writable`、fetch成功、HEAD/`origin/main` は `a40d4bf`。
- [x] 昼runの `3120411` をremote先行分へそのままpushせず、必要差分だけを指定パスへ反映する。Evidence: XGuard commit `d30fc48 Harden Supabase usage ledger boundary`。
- [x] Supabase usage ledger境界を夜レビューで小修正する。Evidence: `record_api_usage_event_with_monthly_limit` の所有関係検証、負値拒否、`service_role` grant、numeric string mapping。
- [x] XGuard実装変更を `UryuAtsuya/Xguard` `origin/main` へpushする。Evidence: `d30fc48`、`a40d4bf..d30fc48 main -> main`。
- [x] 夜レビュー検証を実行する。Evidence: `git diff --check`, `tsc --noEmit`, targeted Vitest（1 file / 4 tests）, `npm run check`（6 files / 37 tests）, `git diff --cached --check` pass。
- [ ] 実Supabase/Postgres migration testでSQL functionのservice-role権限、所有関係、月次上限超過、負値拒否を確認する。Carry-over: `company/todos/2026-05-30.md`

## 夜レビューへ送るTop 3

1. GitHub DNSが通る環境で `origin/main` をfetchし、`3120411` をrebase/mergeして `UryuAtsuya/Xguard` `origin/main` へpushする。Completed evening: `d30fc48`
2. 実Supabase/Postgres migration testで `record_api_usage_event_with_monthly_limit` のservice-role権限、所有関係、月次上限超過、負値拒否を確認する。
3. real OAuth configured modeとDeveloper Console原価確認を閉じ、`docs/API_COST_MODEL.md` とcompany記録へ実値を同期する。

## 後回し

- note記事制作・公開判断
- Today Board改善
- Short Video Operations OS
