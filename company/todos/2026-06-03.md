# TODO - 2026-06-03

## XGuard Top 3

1. [ ] `/Users/uryuatsuya/XGuard/xguard` の `HEAD=origin/main=95e6392` をlive GitHubで再確認し、`git fetch origin main` / `git ls-remote origin refs/heads/main` のDNS・`.git/FETCH_HEAD` blockerを解消する。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`service_role` 実行、`authenticated` 拒否、ownership、同一Xアカウント整合性、存在しない `backup_run`、`x_account_id` 必須、月次上限超過、負値拒否を確認する。
3. [ ] OAuth configured modeの `state` / S256 PKCE / callback validation、token repositoryとSupabase schema契約、Developer Console原価確認を進め、`/api/x/oauth/status` をproductionでadmin-onlyまたはdeployment-onlyへ寄せる。

## 追加候補

- [x] `/api/x/oauth/status` をproductionで無効化、またはadmin-only / deployment-only health checkへ寄せる。昼runで `X_OAUTH_STATUS_DIAGNOSTIC_TOKEN` + `x-xguard-diagnostic-token` header必須に変更し、local commit `9e8b7c5` を作成。pushはremote先行で未完了。
- [ ] OAuth status route testをExpress内部構造直叩きから `supertest` 経由へ置き換える。
- [ ] CORSを `APP_BASE_URL` または明示allowlistベースへ寄せる。

## 昼実装後メモ

- [ ] 指定パス `03ecd2f Default OAuth status diagnostic to disabled` を正として、`9e8b7c5 Guard OAuth status diagnostic in production` に残すべき差分があるか確認する。`9e8b7c5` は丸ごとpushしない。
- [ ] `supertest` による `/api/x/oauth/status` header経路テストは、このsandboxでは `listen EPERM` のため未実施。通常環境で再確認する。

## 夜レビュー後メモ

- [x] 指定パス `/Users/uryuatsuya/XGuard/xguard` の正本を `HEAD=origin/main=03ecd2f Default OAuth status diagnostic to disabled` と確認した。
- [x] `9e8b7c5 Guard OAuth status diagnostic in production` は丸ごとpushしない判断にした。
- [x] `git diff --check`、`git diff --cached --check`、targeted Vitest、`tsc --noEmit`、`npm run test` を確認した。
- [ ] `deployment_diagnostic` 有効時の `/api/x/oauth/status` 無認証公開をheader secret、admin auth、またはprivate health checkで閉じる。
- [ ] `npm run check` は `dist/` write `EPERM` で未完走。writable checkoutまたはCIで再実行する。
