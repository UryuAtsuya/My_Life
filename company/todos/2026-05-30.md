# TODO - 2026-05-30

## XGuard Top 3

1. [ ] 実Supabase/Postgres migration testで `record_api_usage_event_with_monthly_limit` の `service_role` 実行可、`authenticated` 実行不可、所有関係、同一Xアカウント整合性、月次上限超過、負値拒否を確認する。
2. [ ] real OAuth configured modeをsecret非表示で確認し、`/api/x/oauth/start` のconfigured URL、callback URL、scopeが `tweet.read`, `users.read`, `offline.access` のままかを記録する。
3. [ ] Developer Consoleでendpoint別単価、credit/spending設定、Usage endpoint、Owned Reads条件を確認し、`docs/API_COST_MODEL.md`、PM ticket、project noteへ反映する。

## 引き継ぎ

- XGuard正本: `/Users/uryuatsuya/XGuard/xguard`
- XGuard remote: `UryuAtsuya/Xguard` `origin/main`
- 最新push済みcommit: `d30fc48 Harden Supabase usage ledger boundary`
- 2026-05-30朝確認: 指定パスは `exists=yes`, `writable=no`, `main...origin/main`, HEAD `d30fc48`。昼run最初にwrite/fetchを再確認し、書けなければ `/private/tmp/xguard-midday-2026-05-30` をremote最新から作る。
- 2026-05-29夜検証: `git diff --check`, `tsc --noEmit`, targeted Vitest（1 file / 4 tests）, `npm run check`（6 files / 37 tests）, `git diff --cached --check` pass。
- v0 scopeは `tweet.read`, `users.read`, `offline.access` のみ。`follows.read`, DM/write/follow系scopeは入れない。

## 後回し

- note記事制作・公開判断
- Today Board改善
- Short Video Operations OS
