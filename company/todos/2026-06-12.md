---
date: "2026-06-12"
project: "xguard"
type: daily-todo
---

# TODO - 2026-06-12

## XGuard Top 3

1. [x] `PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED` production gate を `runtimeConfig.ts` と `runtimeConfig.test.ts` に実装し、push する。（Codex 昼 run 完了）
   - `RuntimeConfig` に `pricingConfirmed: boolean` / `complianceConfirmed: boolean` を追加
   - production + 両確認 NG 時に `throw new Error("invalid_runtime_env:PRICING_CONFIRMED")` 等を追加
   - `runtimeConfig.test.ts` に 5 ケース追加
   - `git diff --check` + `npx tsc -p tsconfig.json --noEmit` + targeted Vitest 全 pass 後 commit → push
   - 証拠: commit hash と push 結果を `company/notes/2026-06-12-midday-xguard-implementation.md` に記録
   - 完了証跡: XGuard `f27ad55 Add production confirmation gates`、`feature/runtime-confirmation-gates` push 済み、PR #1
2. [ ] Supabase CLI 環境確保（CEO 手動タスク・最終期限）
   - `supabase start` で local Supabase/Postgres を起動する
   - `.env.local` に `DATABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` を設定する
   - 完了後に Codex へ integration test を委譲する
   - 未達成の場合: 理由を `company/decisions/2026-06-12.md` に記録し、production No-Go を確認する
3. [ ] Supabase CLI 確保後: 実 Supabase/Postgres integration test を Codex に委譲する
   - role（service_role 専用実行 / authenticated 拒否）
   - ownership と x_account 整合性
   - 存在しない backup_run
   - 月次上限超過・負値の拒否

## 引き継ぎ事項（2026-06-11 夜会から）

- XGuard 正本: `/Users/uryuatsuya/XGuard/xguard`
- GitHub 反映先: `UryuAtsuya/Xguard`
- XGuard HEAD: `9a7a783 Harden production OAuth callback URL`（= origin/main）
- 未追跡: `output/playwright/` 配下（release gate 全完了まで触らない）
- `npm run check` は `EPERM` 継続。`tsc --noEmit` / targeted Vitest は pass
- `PRICING_CONFIRMED` / `COMPLIANCE_CONFIRMED` gate: runtimeConfig.ts に未実装
- production: No-Go 継続

## CEO 決定事項（参照: company/decisions/2026-06-11.md）

- production No-Go 継続
- Supabase CLI 確保は本日が最終期限
- force push 禁止継続
- `output/playwright/` は release gate 全完了後に .gitignore 追加判断

## 追加候補（優先順 低め）

- [ ] proof visibility endpoint に監査/compliance event 記録を追加する（週次 slice 5）
- [ ] `InMemoryOAuthStateRepository` を TTL 付き atomic consume 可能な永続 repository へ置き換える（週次 slice 4）
- [ ] OAuth configured mode の実 token exchange を live credentials で検証する（週次 slice 3）
- [ ] `output/playwright/` 未追跡ファイルの commit 要否を判断する（Top 3 完了後）
