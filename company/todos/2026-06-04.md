# TODO - 2026-06-04

## XGuard Top 3

1. [ ] `03ecd2f Default OAuth status diagnostic to disabled` を正として、`X_OAUTH_STATUS_EXPOSURE=deployment_diagnostic` 有効時の `/api/x/oauth/status` をheader secret、admin auth、またはprivate health checkに限定する。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`service_role` 実行、`authenticated` 拒否、ownership、同一Xアカウント整合性、`x_account_id` 必須、存在しない `backup_run`、月次上限超過、負値拒否を確認する。
3. [ ] OAuth `state` / S256 PKCE / callback validation / token schema契約を閉じ、Developer ConsoleでX API原価、Usage、Owned Reads条件を確認する。

## 追加候補

- [ ] writable checkoutまたはCIで `npm run check` を再実行する。
- [ ] `/api/x/oauth/status` の404/200/診断制限を `supertest` 等のHTTP境界テストへ置き換える。
- [ ] `git fetch origin main` / `git ls-remote origin refs/heads/main` のDNS・`.git/FETCH_HEAD` blockerを解消し、live GitHubのHEADを再確認する。
