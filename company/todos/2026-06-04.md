# TODO - 2026-06-04

## XGuard Top 3

1. [x] `6024667 Restrict production CORS origins` を起点に、`deployment_diagnostic` 有効時の `/api/x/oauth/status` を32文字以上のheader secret必須へ限定し、HTTP境界テストとsandbox fallbackを追加した。夜の確定HEAD: `394a3c3 Add OAuth diagnostic HTTP boundary tests`。
2. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`service_role` 実行、`authenticated` 拒否、ownership、同一Xアカウント整合性、`x_account_id` 必須、存在しない `backup_run`、月次上限超過、負値拒否を確認する。
3. [ ] OAuth `state` / S256 PKCE / callback validation / token schema契約を閉じ、通常read単価、Enterprise適用要否、24時間削除SLAをXGuard docsへ反映する。

## 追加候補

- [x] 一時作業コピーで `npm run check` を再実行し、`45 passed / 6 skipped tests` を確認した。
- [x] 診断endpoint修正とHTTP境界テストを `394a3c3 Add OAuth diagnostic HTTP boundary tests` として `UryuAtsuya/Xguard` `origin/main` へpushした。
- [ ] 通常CIでsandbox skipされた `supertest` HTTP境界2件を実行する。
- [ ] `docs/API_COST_MODEL.md` で `Owned Reads` を複数顧客向け原価前提から外す。
- [ ] `docs/COMPLIANCE.md` に24時間削除追従とAPI access終了時の全削除runbookを記録する。
- [ ] `git fetch origin main` / `git ls-remote origin refs/heads/main` のDNS・`.git/FETCH_HEAD` blockerを解消し、live GitHubのHEADを再確認する。
- [x] CORSは `6024667 Restrict production CORS origins` で `APP_BASE_URL` / `CORS_ORIGINS` ベースへ進展済み。明日はregression確認だけでよい。

## 夜レビュー後メモ

- [x] `git diff --check`, `git diff --cached --check`, `tsc --noEmit`, `npm run test` を確認した。全testは `50 passed / 4 skipped`。
- [ ] `npm run build` / `npm run check` は `dist/backend/...` write `EPERM` で未完走。
- [ ] OAuth固定 `state` / plain PKCE / callback未照合を解消する。
- [ ] backup / proof APIに認証・user ownership・proof visibility/revocation境界を追加する。
