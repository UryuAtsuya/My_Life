---
date: "2026-05-26"
type: daily
---

# 2026-05-26 (Tue)

## XGuard Top 3

1. [ ] XGuard指定パス `/Users/uryuatsuya/XGuard/xguard` の書き込み可否を確認し、不可なら `/private/tmp/xguard-midday-2026-05-26` の一時cloneでremote `b3bd37c` とlocal `f60be3e`/`91229db` 相当を統合してpushする。 | Priority: P0 | Evidence needed: final commit hash and `git push -v origin main` success
2. [ ] `backend/src/__tests__/tokenRepository.test.ts` のmock fetch型を修正し、`SupabaseTokenRepository.findXToken()` のread pathで `assertReadOnlyXScopes(row.scope)` を呼ぶ。 | Priority: P1 | Evidence needed: `npx tsc -p tsconfig.json --noEmit`, `npx vitest run --configLoader runner`, and `npm run check` result
3. [ ] Developer Consoleでendpoint別単価、spending limit、Usage endpointを確認し、`docs/API_COST_MODEL.md` と `api_usage_events` / `backup_runs` の最小contractへ実値または未確認理由を反映する。 | Priority: P1 | Evidence needed: copied settings values or screenshot reference, plus cost ledger diff

## Carry-over

- [ ] Stripe webhook handlerを `stripe_events.event_id` unique前提で冪等に処理する。 | Priority: P2
- [ ] `backup_runs` + `api_usage_events` transaction serviceを実装し、rate-limit metadataとestimated costを保存する。 | Priority: P2
