---
date: "2026-05-26"
type: daily
---

# 2026-05-26 (Tue)

## XGuard Top 3

1. [ ] XGuard指定パス `/Users/uryuatsuya/XGuard/xguard` でremote `origin/main` をfetchできる状態にし、GitHub上の `b3bd37c` とローカル `f60be3e`/`91229db` を統合してpushする。 | Priority: P0 | Evidence needed: final commit hash and `git push -v origin main` success
2. [ ] `backend/src/__tests__/tokenRepository.test.ts` のmock fetch型を修正し、`SupabaseTokenRepository.findXToken()` のread pathで `assertReadOnlyXScopes(row.scope)` を呼ぶ。 | Priority: P1 | Evidence needed: `npx tsc -p tsconfig.json --noEmit` and `npm run check` pass
3. [ ] Developer Consoleでendpoint別単価、spending limit、Usage endpointを確認し、`docs/API_COST_MODEL.md` とcompany gateへ実値または未確認理由を反映する。 | Priority: P1 | Evidence needed: copied settings values or screenshot reference

## Carry-over

- [ ] Stripe webhook handlerを `stripe_events.event_id` unique前提で冪等に処理する。 | Priority: P2
- [ ] `backup_runs` + `api_usage_events` transaction serviceを実装し、rate-limit metadataとestimated costを保存する。 | Priority: P2
