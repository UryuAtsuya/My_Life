# TODO - 2026-06-05

## XGuard Top 3

1. [ ] OAuth configured modeに一回限り `state`、S256 PKCE、callback validation、TTL、replay防止を実装し、forged / expired / replay callbackテストを追加する。
2. [ ] backup / proof APIへ認証・user ownership・proof visibility/revocation境界を追加する。
3. [ ] 実Supabase/Postgresで `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1` を実行し、`docs/API_COST_MODEL.md` と `docs/COMPLIANCE.md` の商用release gateを反映する。

## 追加候補

- [ ] configured OAuthのfrontendを `authorizationUrl` へ遷移させ、frontend testを追加する。
- [ ] 通常CIでsandbox skipされた診断endpointの `supertest` HTTP境界2件を実行する。
- [ ] `394a3c3` を起点に通常CIでsandbox skipされた診断endpointの `supertest` HTTP境界2件を実行する。
- [ ] token repositoryとSupabase schemaの保存契約を一本化する。
