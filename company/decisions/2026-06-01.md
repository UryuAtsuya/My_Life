---
date: "2026-06-01"
type: decisions
---

# Decisions - 2026-06-01

## Morning XGuard research meeting

1. 今日の事業最優先はXGuardにする。note/Today Board/その他webサービス改善は、XGuardのlive remote確認、実Supabase/Postgres migration test、Developer Console原価確認が進むまで後回しにする。
2. XGuard v0は、引き続き「BAN復活」ではなく「本人が事前同意したXデータのread-only backup」と「BAN後の手動再起動を支えるproof page」に限定する。
3. v0初期OAuth scopeは `tweet.read`, `users.read`, `offline.access` に維持する。`follows.read`, `dm.read`, `dm.write`, `tweet.write`, `follows.write` は初期同意画面に入れない。
4. 指定パス `/Users/uryuatsuya/XGuard/xguard` のlocal `HEAD` / local `origin/main` は `2655267 Filter revoked tweet snapshots from proof DTO`。ただし `git ls-remote` はDNS失敗、`git fetch` は `.git/FETCH_HEAD` 書き込み不可のため、live remote確認は未完了として扱う。
5. 昼の最初のゲートは `/Users/uryuatsuya/XGuard/xguard` のwrite/fetch/live remote確認にする。書けない場合は `/private/tmp/xguard-midday-2026-06-01` を使い、MyLife Vaultへ実装コードを置かない。
6. `record_api_usage_event_with_monthly_limit` は、実Supabase/Postgres上でも `service_role` 専用実行、`authenticated` 拒否、所有関係検証、同一Xアカウント整合性、存在しない `backup_run`、月次上限超過、負値拒否を確認するまで完了扱いにしない。
7. X API原価はDeveloper Console実画面確認を正とする。確認できるまで、通常read単価と月次原価上限を保守的に使い、`Owned Reads` を第三者ユーザー向けSaaSの主前提にしない。
8. proof pageはraw X API payloadを公開せず、公開DTO、visibility、revocation、redaction、X Content削除/非公開/withheld追従を実装境界に入れる。
9. 自動DM、自動follow/unfollow、自動投稿、bulk outreach、BAN回避に見える新アカウント自動作成・誘導はv0から外す。
10. `/api/x/oauth/status` は本番公開前にadmin認証付きhealth checkまたはdeployment-only routeへ寄せる。

## Midday XGuard implementation

11. 実Supabase/Postgresへ到達できない昼runでは、`record_api_usage_event_with_monthly_limit` のmigration前提をlocal schema contract testで強化する。ただし実DB上の `service_role` 実行、`authenticated` 拒否、RLS / grant / check constraint の挙動確認までは完了扱いにしない。
12. `backup_run_id` 付き usage event は `x_account_id` 必須とし、`backup_runs.x_account_id = p_x_account_id` を常に要求する。`x_account_id` がnullのusage eventをbackup runに紐づける形は、同一Xアカウント整合性を曖昧にするため採用しない。
13. XGuard local commit `8cf029c Harden usage ledger schema contract` はDNS失敗で未push。GitHub接続復旧後にforce pushなしで統合する。
