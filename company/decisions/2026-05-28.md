---
date: "2026-05-28"
type: decisions
---

# Decisions - 2026-05-28

## Morning XGuard research meeting

1. 今日の事業最優先はXGuardにする。note/Today Board/その他webサービス改善は、指定パス同期、Developer Console原価確認、Supabase ledger repository化が進むまで後回しにする。
2. XGuard v0は、引き続き「BAN復活」ではなく「本人が事前同意したXデータのread-only backup」と「BAN後の手動再起動を支えるproof page」に限定する。
3. v0初期OAuth scopeは `tweet.read`, `users.read`, `offline.access` に維持する。`follows.read`, `dm.read`, `dm.write`, `tweet.write`, `follows.write` は初期同意画面に入れない。
4. X API原価はDeveloper Console実画面確認を正とする。確認できるまで、Post read `$0.005/resource`、User/Followers read `$0.010/resource` の保守的値で見る。
5. `Owned Reads` は安いが、公式pricing上は「developer app owner自身のデータ」が条件なので、第三者ユーザー向けSaaSの通常ユーザーに適用できると確認できるまで主前提にしない。
6. proof pageはraw X API payloadを公開せず、公開DTO、visibility、revocation、redaction、X Content削除/非公開/withheld追従を実装境界に入れる。
7. 自動DM、自動follow/unfollow、自動投稿、bulk outreach、BAN回避に見える新アカウント自動作成・誘導はv0から外す。
8. 昼の最初のゲートは `/Users/uryuatsuya/XGuard/xguard` の書き込み可否と、push済み正本 `3528e26` への同期にする。朝run時点では `exists=yes`, `writable=no`, `main...origin/main [ahead 1]`, HEAD `e750d04`。
9. 昼の実装対象は、指定パス同期が無理なら `/private/tmp/xguard-midday-2026-05-28` の一時cloneで `SupabaseApiUsageLedgerRepository` と月次API原価上限stop ruleを実装する。

## Midday XGuard implementation

1. 指定パス `/Users/uryuatsuya/XGuard/xguard` は今日は書き込み可能だったため、一時cloneではなく指定パスを正本として実装する。
2. `origin/main` `3528e26` とlocal `e750d04` はmergeして扱う。local単独commitをそのままpushせず、remote正本のledger validationを取り込んだうえで進める。
3. prototypeの次のenv gateは `X_CLIENT_ID` を最小スイッチにする。`X_CALLBACK_URL` は明示設定を優先し、未設定時は `APP_BASE_URL` またはlocal portから組み立てる。
4. `X_CLIENT_SECRET` はruntimeで検知するが、現時点ではtoken exchangeしない。callbackはまだrepository reference保存のprototype境界に留める。
5. XGuard実装repoの昼反映は `18676f0 Add runtime OAuth configuration` を `UryuAtsuya/Xguard` `origin/main` へpush済みとする。
