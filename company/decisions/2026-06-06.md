---
date: "2026-06-06"
---

# Decisions - 2026-06-06

## XGuard evening code review

- 今日のXGuard正本は `/Users/uryuatsuya/XGuard/xguard`、反映先は `UryuAtsuya/Xguard` `origin/main` とする。local `HEAD` と local tracking `origin/main` は `2b96993 Add OAuth state and PKCE guard`。
- OAuth configured modeの一回限り `state`、S256 PKCE、TTL、replay防止は反映済みとして扱う。
- production releaseはNo-Go継続。理由はbackup / proof APIの認証・所有権・visibility/revocation、実Supabase/Postgres検証、cost/compliance release gateが未完のため。
- 夜間の小修正は行わない。backup/proof境界はauth設計とテストが必要なため、明日のTop 1へ送る。
- `output/playwright/` の未追跡ファイルは今回触らず、次回実装前にcommit対象か削除対象か判断する。

## XGuard morning research refresh

- XGuardは今日も事業最優先。OAuth `state` / S256 PKCE は反映済みとして、backup / proof APIの認証・所有権・visibility/revocationをTop 1にする。
- X APIはpay-per-usage、Usage endpoint、spending limit、rate limit header、Developer Console実値確認を原価管理の中心にする。
- `Owned Reads` は複数顧客向けXGuardの主原価前提にしない。通常read単価と月次上限前停止を設計に入れる。
- X Contentの削除・変更・非公開・停止への24時間追従、API access終了時の全削除runbook、Enterprise適用要否確認をproduction release gateにする。
- v0 scopeは `tweet.read`, `users.read`, `offline.access` のread-only backupとproof pageに限定し、自動DM、自動follow/unfollow、自動投稿、bulk outreach、BAN回避導線は作らない。

## XGuard 17:00 morning automation rerun

- 同日昼実装で backup / proof APIの所有権・visibility/revocation境界は `d5aa75e Guard backup proof ownership boundaries` まで進んだため、残Top 1を `d5aa75e` のpushへ切り替える。
- GitHub反映はDNS/fetch状態を確認してから行い、remote先行分がある場合は取り込んでからpushする。force pushはしない。
- `Owned Reads` はdeveloper app owner本人向け条件が明記されているため、XGuardの複数顧客向け原価モデルでは通常read単価を採用する。
- proof/backup routeのprototype user headerは本番認証ではなく、次のrelease gateでSupabase Auth/JWT + DB ownershipへ置き換える。
