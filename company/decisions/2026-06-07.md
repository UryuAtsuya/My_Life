---
date: "2026-06-07"
---

# Decisions - 2026-06-07

## XGuard 朝会 CEO 判断

- 今日の最優先は XGuard。明確に緊急な別件なし。
- XGuard 正本は `/Users/uryuatsuya/XGuard/xguard`、反映先は `UryuAtsuya/Xguard` `origin/main`。local `HEAD` と local tracking `origin/main` は `2b96993 Add OAuth state and PKCE guard`。
- production release は No-Go 継続。理由は backup / proof API の認証・所有権・visibility/revocation、実 Supabase/Postgres 検証、cost/compliance release gate が未完のため。

### 今日の Top 3 確定

1. **backup / proof API 認証・所有権・proof visibility/revocation 境界の実装**（Codex へ委譲）
   - `POST /api/backup/run`、`GET /api/backup/status/:runId`、`GET /api/recovery/:runId/proof` は現在無認証。
   - 他 user・private proof・revoked proof 拒否テストを HTTP 境界で追加する。
   - 完了定義: auth middleware 適用済み、拒否テスト全 pass、TypeScript エラーなし。

2. **実 Supabase/Postgres で usage ledger integration test を実行**（Codex へ委譲）
   - `RUN_SUPABASE_SQL_INTEGRATION_TESTS=1 npx vitest run --configLoader runner backend/src/__tests__/supabaseSqlApiUsageLedger.integration.test.ts` を実 DB 接続で実行する。
   - 確認項目: role、ownership、同一 X アカウント、存在しない `backup_run`、`x_account_id` 必須、負値、月次上限超過。

3. **docs gate 反映**（Codex へ委譲）
   - `docs/API_COST_MODEL.md`: 通常 read 単価、`Owned Reads` 非適用根拠、Usage endpoint、spending limit、月次上限前停止。
   - `docs/COMPLIANCE.md`: Enterprise 適用要否確認、24 時間削除 SLA、API access 終了時全削除 runbook。

### 継続判断

- `output/playwright/` 未追跡ファイルは Top 3 完了後に commit 対象か削除対象か判断する。今日は触らない。
- `InMemoryOAuthStateRepository` は prototype 範囲として許容。本番前に共有永続 store へ置き換える判断は launch タイミング確定後に行う。
- `npm run build:api` / `npm run build:web` の `EPERM` は指定パス権限の問題。Codex 昼 run が `/private/tmp/xguard-midday-2026-06-07` を使う場合は build 検証を `/private/tmp` 側で完了させる。
- persona / pricing LP 文言案（`Free診断 / 1,980円 Personal / 2,980円 Pro / 4,980円 Concierge検証枠`）は Top 1-3 が完了次第、次のサイクルで取り上げる。今日は実装優先。
- 初期ヒアリング仮リスト作成（X 中心 creator 等）も同様に次サイクルへ送る。

### git 状態（朝会確認）

- MyLife repo: `main` は `origin/main` と同期済み。未追跡ファイルあり（`Projects/`, `Ideas/`, `.obsidian/` 等）が commit 対象外。
- XGuard repo: 前回夜会での `git ls-remote` DNS 失敗・`git fetch` 権限 blocker は未解消。Codex 昼 run でも同様の制約が予想される。GitHub live remote 確認は CI 環境での実行を推奨。

## XGuard 夜レビュー判断

- XGuard `9ac4f2f Add proof visibility management route` は `UryuAtsuya/Xguard` `main` へpush済みとして扱う。push出力は `b03d9c8..9ac4f2f main -> main`。
- production releaseはNo-Go継続。理由は、OAuth configured modeの実X token exchange未実装、実Supabase/Postgres検証未完、cost/compliance release gate未完。
- 明日の最優先を「OAuth configured modeで実token exchange + subject/account検証 + production mock callback禁止」に変更する。
- backup / proofのprivate defaultは完了扱い。ただしowner-only proof visibility update APIと監査記録はP2として残す。
- 追記: owner-only proof visibility update APIは `9ac4f2f` で反映済み。次はOAuth実token exchangeへ進む。
